# 项目协作规则

## 本地提交

- 每次完成一个用户请求的功能或修复后，运行与改动相匹配的验证，并创建本地 Git 提交。
- 遵循 Conventional Commits 格式：`type(scope): 中文描述`。
- `type` 使用 `feat`、`fix`、`refactor`、`test`、`docs` 或 `chore` 等标准类型；`scope` 使用受影响模块的简短英文名称。
- 提交说明必须使用中文，简明描述单一功能改动；不要在同一提交混入无关修改。
- 提交前检查暂存区，确保不提交构建产物、临时文件或用户未授权的改动。
